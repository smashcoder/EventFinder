function redirect() {
        // Define the relative path to the destination page in the same folder
        var redirectTo = "register.html"; // Replace with the actual filename

        // Perform the redirect
        window.location.href = redirectTo;
    }